# 🏗️ Arquitectura del Sistema - Bites ERP

## Vista General del Sistema

```
┌─────────────────────────────────────────────────────────────────┐
│                         BITES ERP SYSTEM                        │
│                  Sistema de Gestión para Restaurantes           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────┐          ┌──────────────────────────┐
│                          │          │                          │
│   FRONTEND (React)       │  HTTP    │   BACKEND (Node.js)      │
│   Puerto: 5173           │◄────────►│   Puerto: 5000           │
│                          │  REST    │                          │
│   - React 19             │   API    │   - Express.js           │
│   - React Router         │          │   - JWT Auth             │
│   - Tailwind CSS         │          │   - RBAC                 │
│   - Vite                 │          │   - Mongoose             │
│                          │          │                          │
└──────────────────────────┘          └────────────┬─────────────┘
                                                   │
                                                   │ MongoDB
                                                   │ Driver
                                                   │
                                      ┌────────────▼─────────────┐
                                      │                          │
                                      │   MongoDB Database       │
                                      │   Puerto: 27017          │
                                      │                          │
                                      │   - users                │
                                      │   - restaurantes         │
                                      │   - productos            │
                                      │   - inventarios          │
                                      │   - ordenes              │
                                      │   - mesas                │
                                      │   - reservas             │
                                      │                          │
                                      └──────────────────────────┘
```

---

## Arquitectura del Backend (MVC)

```
┌──────────────────────────────────────────────────────────────────┐
│                        BACKEND - API REST                        │
└──────────────────────────────────────────────────────────────────┘

    Request (HTTP)
         │
         ▼
┌─────────────────────┐
│   MIDDLEWARES       │
│                     │
│  ┌───────────────┐  │
│  │ CORS          │  │  ← Seguridad CORS
│  ├───────────────┤  │
│  │ Helmet        │  │  ← Headers de seguridad
│  ├───────────────┤  │
│  │ Morgan        │  │  ← Logs de peticiones
│  ├───────────────┤  │
│  │ Body Parser   │  │  ← Parse JSON
│  └───────────────┘  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   ROUTES            │
│                     │
│  /api/auth          │  ← Autenticación
│  /api/users         │  ← Usuarios/Empleados
│  /api/productos     │  ← Productos (próximo)
│  /api/inventario    │  ← Inventario (próximo)
│  /api/ordenes       │  ← Órdenes (próximo)
│  /api/mesas         │  ← Mesas (próximo)
│  /api/reservas      │  ← Reservas (próximo)
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│  AUTH MIDDLEWARE    │
│                     │
│  ┌───────────────┐  │
│  │ protect       │  │  ← Verificar JWT
│  ├───────────────┤  │
│  │ authorize     │  │  ← Verificar rol
│  ├───────────────┤  │
│  │checkPermission│  │  ← Verificar permiso
│  └───────────────┘  │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   VALIDATORS        │
│                     │
│  - validateRegister │
│  - validateLogin    │
│  - validateEmployee │
│  - validateUpdate   │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   CONTROLLERS       │
│                     │
│  auth.controller    │  ← Lógica de auth
│  user.controller    │  ← Lógica de usuarios
│  ...                │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   MODELS            │
│                     │
│  User.js            │  ← Esquema de usuario
│  Restaurante.js     │  ← Esquema de restaurante
│  ...                │
└──────────┬──────────┘
           │
           ▼
┌─────────────────────┐
│   MONGODB           │
│                     │
│  Operaciones CRUD   │
└──────────┬──────────┘
           │
           ▼
    Response (JSON)
```

---

## Flujo de Autenticación

```
┌────────────┐
│   CLIENTE  │
└──────┬─────┘
       │
       │ 1. POST /api/auth/register
       │    { email, password, restaurante }
       │
       ▼
┌──────────────────────┐
│   validateRegister   │  ← Validar datos
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│  auth.controller     │
│  register()          │
└──────┬───────────────┘
       │
       │ 2. Verificar email único
       │
       ▼
┌──────────────────────┐
│   Crear Restaurante  │  ← Mongoose
└──────┬───────────────┘
       │
       ▼
┌──────────────────────┐
│   Crear Usuario      │  ← bcrypt hashea password
│   rol: 'admin'       │
└──────┬───────────────┘
       │
       │ 3. Generar JWT
       │
       ▼
┌──────────────────────┐
│   jwt.sign()         │
│   { id: userId }     │
└──────┬───────────────┘
       │
       │ 4. Retornar token + datos
       │
       ▼
┌──────────────────────┐
│  Response:           │
│  {                   │
│    success: true,    │
│    token: "...",     │
│    user: {...}       │
│  }                   │
└──────┬───────────────┘
       │
       ▼
┌────────────┐
│   CLIENTE  │  ← Guarda token
└────────────┘
```

---

## Flujo de Petición Protegida

```
┌────────────┐
│   CLIENTE  │
└──────┬─────┘
       │
       │ GET /api/users
       │ Authorization: Bearer <token>
       │
       ▼
┌──────────────────────┐
│  protect middleware  │
└──────┬───────────────┘
       │
       │ 1. Extraer token del header
       │
       ▼
┌──────────────────────┐
│  jwt.verify()        │  ← Verificar token válido
└──────┬───────────────┘
       │
       │ 2. Token válido
       │
       ▼
┌──────────────────────┐
│  Buscar Usuario      │  ← User.findById()
└──────┬───────────────┘
       │
       │ 3. Usuario encontrado
       │    req.user = usuario
       │
       ▼
┌──────────────────────┐
│  authorize/          │  ← Verificar rol/permiso
│  checkPermission     │
└──────┬───────────────┘
       │
       │ 4. Permiso otorgado
       │
       ▼
┌──────────────────────┐
│  user.controller     │  ← Ejecutar lógica
│  getUsers()          │
└──────┬───────────────┘
       │
       │ 5. Consultar DB
       │
       ▼
┌──────────────────────┐
│  User.find()         │  ← Filtrar por restaurante
└──────┬───────────────┘
       │
       │ 6. Retornar datos
       │
       ▼
┌────────────┐
│   CLIENTE  │
└────────────┘
```

---

## Sistema RBAC (Control de Acceso)

```
┌────────────────────────────────────────────────────────────┐
│                     ROLES HIERARCHY                         │
└────────────────────────────────────────────────────────────┘

                      ┌──────────┐
                      │  ADMIN   │  ← Acceso total
                      └─────┬────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌────▼────┐         ┌────▼────┐        ┌────▼────┐
   │ MESERO  │         │COCINERO │        │  CAJERO │
   └─────────┘         └─────────┘        └─────────┘
        │
   ┌────▼────┐
   │  HOST   │
   └─────────┘

┌────────────────────────────────────────────────────────────┐
│                   PERMISSIONS MATRIX                       │
└────────────────────────────────────────────────────────────┘

Módulo        │ Admin │ Mesero │ Cocinero │ Cajero │ Host
──────────────┼───────┼────────┼──────────┼────────┼──────
Empleados     │  CRUD │   -    │    -     │   -    │  -
Productos     │  CRUD │   R    │    R     │   R    │  -
Inventario    │  CRUD │   -    │    R     │   -    │  -
Órdenes       │  CRUD │   CRU  │   RU     │   RU   │  -
Mesas         │  CRUD │   RU   │    -     │   -    │  RU
Reservas      │  CRUD │   -    │    -     │   -    │  CRUD
Perfil        │   RU  │   RU   │   RU     │   RU   │  RU

Leyenda:
C = Create (Crear)
R = Read (Leer)
U = Update (Actualizar)
D = Delete (Eliminar)
- = Sin acceso
```

---

## Estructura de Datos (MongoDB)

```
┌──────────────────────────────────────────────────────────┐
│                      USERS Collection                    │
├──────────────────────────────────────────────────────────┤
│  _id: ObjectId                                           │
│  nombre: String                                          │
│  apellido: String                                        │
│  email: String (unique per restaurant)                   │
│  password: String (hashed)                               │
│  rol: String [admin, mesero, cocinero, cajero, host]     │
│  restauranteId: ObjectId → Restaurante                   │
│  telefono: String                                        │
│  foto: String (URL)                                      │
│  activo: Boolean                                         │
│  creadoPor: ObjectId → User                              │
│  ultimoAcceso: Date                                      │
│  createdAt: Date                                         │
│  updatedAt: Date                                         │
└──────────────────────────────────────────────────────────┘
                            │
                            │ N:1
                            ▼
┌──────────────────────────────────────────────────────────┐
│                 RESTAURANTES Collection                  │
├──────────────────────────────────────────────────────────┤
│  _id: ObjectId                                           │
│  nombre: String                                          │
│  descripcion: String                                     │
│  telefono: String                                        │
│  email: String                                           │
│  direccion: Object {                                     │
│    calle, ciudad, estado, codigoPostal, pais             │
│  }                                                       │
│  logo: String (URL)                                      │
│  moneda: String                                          │
│  horarios: Object                                        │
│  adminId: ObjectId → User                                │
│  activo: Boolean                                         │
│  createdAt: Date                                         │
│  updatedAt: Date                                         │
└──────────────────────────────────────────────────────────┘

PRÓXIMAS COLECCIONES:
- productos
- categorias
- inventarios
- ordenes
- items_orden
- mesas
- reservas
- pagos
```

---

## Seguridad y Validaciones

```
┌──────────────────────────────────────────────────────────┐
│                    CAPAS DE SEGURIDAD                    │
└──────────────────────────────────────────────────────────┘

1. FRONTEND
   └─► Validación básica de formularios
   └─► Ocultación de opciones según rol
   └─► Redirección si no está autenticado

2. NETWORK
   └─► HTTPS (en producción)
   └─► CORS configurado
   └─► Rate limiting (próximamente)

3. MIDDLEWARE
   └─► Helmet (Headers de seguridad)
   └─► express-validator (Validación de datos)
   └─► JWT verification
   └─► Role-based access control

4. CONTROLLER
   └─► Validaciones de negocio
   └─► Verificación de pertenencia (mismo restaurante)
   └─► Sanitización de datos

5. DATABASE
   └─► Esquemas de Mongoose con validaciones
   └─► Índices únicos
   └─► Referencias con populate

6. DATA STORAGE
   └─► Passwords hasheados (bcrypt)
   └─► Soft deletes (no se eliminan registros)
   └─► Auditoría (creadoPor, timestamps)
```

---

## Flujo de Registro y Onboarding

```
┌─────────────────────────────────────────────────────────┐
│           USER REGISTRATION & ONBOARDING                 │
└─────────────────────────────────────────────────────────┘

1. Usuario visita la página
   │
   ▼
2. Completa formulario de registro
   - Nombre y apellido
   - Email
   - Contraseña
   - Nombre del restaurante
   │
   ▼
3. Frontend valida datos
   │
   ▼
4. POST /api/auth/register
   │
   ▼
5. Backend valida datos
   │
   ▼
6. Se crea el Restaurante
   │
   ▼
7. Se crea el Usuario con rol 'admin'
   │
   ▼
8. Se genera JWT token
   │
   ▼
9. Usuario recibe token
   │
   ▼
10. Redirige a Dashboard
    │
    ▼
11. Admin puede empezar a:
    - Agregar empleados
    - Configurar productos
    - Configurar mesas
    - Iniciar operaciones
```

---

## Escalabilidad y Futuras Mejoras

```
┌─────────────────────────────────────────────────────────┐
│              ARQUITECTURA FUTURA (v2.0)                 │
└─────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐
│   Frontend   │         │   Admin      │
│   Web App    │         │   Dashboard  │
└──────┬───────┘         └──────┬───────┘
       │                        │
       └────────┬───────────────┘
                │
                ▼
        ┌───────────────┐
        │  API Gateway  │  ← Rate limiting, caching
        └───────┬───────┘
                │
       ┌────────┴────────┐
       │                 │
       ▼                 ▼
┌─────────────┐   ┌─────────────┐
│  Auth       │   │  Business   │
│  Service    │   │  Service    │
└──────┬──────┘   └──────┬──────┘
       │                 │
       └────────┬────────┘
                │
       ┌────────┴────────┬──────────┐
       │                 │          │
       ▼                 ▼          ▼
┌──────────┐      ┌──────────┐  ┌────────┐
│ MongoDB  │      │  Redis   │  │ AWS S3 │
│ (Main DB)│      │ (Cache)  │  │(Images)│
└──────────┘      └──────────┘  └────────┘

Mejoras Planificadas:
- Microservicios
- Cache con Redis
- Queue system (RabbitMQ/Bull)
- WebSockets para tiempo real
- CDN para assets
- Load balancing
- Docker + Kubernetes
- CI/CD Pipeline
```

---

## Tecnologías Utilizadas

### Frontend

- **React 19**: Biblioteca de UI
- **React Router DOM**: Navegación
- **Tailwind CSS 4**: Estilos utility-first
- **Vite**: Build tool ultra-rápido

### Backend

- **Node.js**: Runtime de JavaScript
- **Express.js**: Framework web
- **MongoDB**: Base de datos NoSQL
- **Mongoose**: ODM para MongoDB
- **JWT**: Autenticación stateless
- **bcryptjs**: Hash de contraseñas
- **express-validator**: Validación de datos
- **helmet**: Seguridad HTTP
- **cors**: Cross-Origin Resource Sharing
- **morgan**: HTTP request logger

### DevOps & Tools

- **nodemon**: Auto-reload en desarrollo
- **MongoDB Compass**: GUI para MongoDB
- **REST Client**: Testing de API en VS Code
- **Git**: Control de versiones

---

## 📚 Documentación Relacionada

- `README.md` - Documentación general del proyecto
- `backend/README.md` - Documentación detallada del backend
- `backend/RBAC_DOCS.md` - Sistema de roles y permisos
- `backend/MONGODB_SETUP.md` - Instalación y configuración de MongoDB
- `QUICK_START.md` - Guía rápida de inicio

---

**Fecha de última actualización**: Noviembre 2025  
**Versión**: 1.0.0
